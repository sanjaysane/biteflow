pytest>=8.0
# Development / CI-only dependencies. The Docker image and runtime
# install requirements.txt only; these are for lint, tests, and load.
-r requirements.txt
ruff>=0.8
pytest-cov>=6.0
locust>=2.46.4
